<!DOCTYPE struts PUBLIC
"-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
"http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>
    
    <constant name="struts.devMode" value="false" />

    <package name="basicstruts2" extends="struts-default" namespace="/">
        <action name="validarLogin" class="es.ubu.alu.mydatabasejc.actions.Login" method="validarLogin">
            <result name="error">/login.jsp</result>
            <result>/inicio.jsp</result>
        </action>
        <action name="login" class="es.ubu.alu.mydatabasejc.actions.Login" method="login">
            <result name="error">/login.jsp</result>
            <result>/inicio.jsp</result>
        </action>
    </package>

    <!--include file="accestats.xml"/-->
    <!-- Configuration for the default package. -->
    <!--package name="default" extends="struts-default">
    </package>
    <constant name="struts.devMode" value="false"/>
    <constant name="struts.excludedClasses" value="com.opensymphony.xwork2.ActionContext" /-->
 
    <!-- this must be valid regex, each '.' in package name must be escaped! -->
    <!-- it's more flexible but slower than simple string comparison -->
    <!-- constant name="struts.excludedPackageNamePatterns" value="^java\.lang\..*,^ognl.*,^(?!javax\.servlet\..+)(javax\..+)" / -->
 
    <!-- this is simpler version of the above used with string comparison -->
    <!--constant name="struts.excludedPackageNames" value="java.lang,ognl,javax" />
    <constant name="struts.ognl.allowStaticMethodAccess" value="false" /-->
</struts>
